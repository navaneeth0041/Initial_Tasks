1. Write an assert statement that triggers an AssertionError if the variable spam is an integer less than 10.
ans,
	assert spam>=10, 'The variable is less than 10'

2. Write an assert statement that triggers an AssertionError if the variables eggs and bacon contain strings that are the same as each other, even if their cases are different (that is, 'hello' and 'hello' are considered the same, and 'goodbye' and 'GOODbye' are also considered the same).
ans,
	assert eggs.lower()!=bacon.lower() ,'They are the same'
	assert eggs.upper()!=bacon.upper() ,'They are the same'

3. Write an assert statement that always triggers an AssertionError
ans,
	assert False,'This happens always'

4. What are the two lines that your program must have in order to be able to call logging.debug()?
ans,
	import logging
	logging.basicConfig(level=logging.DEBUG,format='%(asctime)s-%(levelname)s-%(message)s')

5. What are the two lines that your program must have in order to have logging.debug() send a logging message to a file named programLog.txt?
ans,
	import logging
	logging.basicConfig(level=logging.DEBUG,format='%(asctime)s-%(levelname)s-%(message)s')

6. What are the five logging levels?
ans,
	DEBUG,INFO,WARNING,ERROR,CRITICAL

7.  What line of code can you add to disable all logging messages in your program?
ans,
	logging.disable(logging.DEBUG)

8. 	Why is using logging messages better than using print() to display the same message?
ans,
	Easy to disable the messages accoding to our convinience

9. What are the differences between the Step Over, Step In, and Step Out buttons in the debugger?
ans,
	Step Over: Execute the current line of code 
	Step In : Allow to execute the next function call in the current line
	Step Out : Execute the remaining lines of the current function

10. After you click Continue, when will the debugger stop?
ans,
	Stop at the end of the program

11. What is a breakpoint?
ans,
	A code which makes the debugge stop on reaching that line of code

12. How do you set a breakpoint on a line of code in Mu?
ans,
	Click to make a red dot appear 